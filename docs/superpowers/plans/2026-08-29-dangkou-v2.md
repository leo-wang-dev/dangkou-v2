# 档口商品管理 v2.0 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 PRD-v2.0 实现品类模板驱动的商品系统（剃须刀/卷发棒物理表 + 导入四分类审批 + H5 模板 CRUD + 图检索 + 报价单导出 + 验收 Harness）。

**Architecture:** 全新 FastAPI+SQLite 侧车（dangkou-v2），品类物理表列即模板字段；dsh-engine 挂 catalog-v2 插件（微信入口、Sub Agent 异步解析、回调推送）；图片先本地存储经存储接口抽象；检索用百炼图向量。

**Tech Stack:** Python 3.12 / FastAPI / SQLite / openpyxl / requests；Node 22（engine 插件 mjs）；百炼 qwen3-vl-embedding；claude CLI（sonnet-5@ccdox，env 可换）。

## Global Constraints

- 仓库：`/Users/elias/code/eryuan/dangkou-v2`，main 分支直接提交，每任务一 commit
- 模板字段以 `catalog/templates.py` 为唯一事实源，表结构/H5/提示词全部从它生成，禁止各处硬编码字段
- 一切写操作（导入/修改/删除）必经 `approval_ticket`，AI 不直接落库
- 内部货号格式 `KS-` + 8 位去混淆字符（`ABCDEFGHJKLMNPQRSTUVWXYZ23456789`）
- 单元测试不调真实 LLM/向量 API——Agent 与检索外呼只在 Harness（M5）跑真货
- 旧仓库 `/Users/elias/code/eryuan/dangkou` 只读参考，可搬实现，不 import
- env 经 `catalog/config.py` 自加载 `.env`（键见 Task 1），密钥不进 git

## 文件结构（职责锁定）

```
dangkou-v2/
├── catalog/
│   ├── config.py      # env 加载与常量
│   ├── templates.py   # 品类模板常量（唯一事实源）
│   ├── schema.sql     # 建表 DDL（由模板生成列）
│   ├── db.py          # 连接/init_db/行映射
│   ├── inner_code.py  # KS-XXXXXXXX
│   ├── storage.py     # 图片存储接口（Local 实现）
│   ├── classify.py    # 导入四分类
│   ├── agent.py       # claude -p 解析（硬约束提示词+超时拯救）
│   ├── tickets.py     # 审批工单+一次性token
│   ├── ingest.py      # 异步导入编排+回调
│   ├── search.py      # 图向量+余弦+换一批
│   ├── quote.py       # 报价单xlsx生成
│   ├── api.py         # FastAPI 路由
│   └── main.py        # app 组装入口
├── static/index.html  # H5 审核页+商品页
├── engine-plugin/catalog-v2.mjs
├── tests/             # 单测（不外呼）
├── harness/           # 验收（外呼真货）
├── docs/PRD-v2.0.md
└── requirements.txt
```

---

## M1 基础框架

### Task 1: 仓库骨架 + 配置 + 建表

**Files:**
- Create: `requirements.txt`, `.env.example`, `catalog/__init__.py`, `catalog/config.py`, `catalog/schema.sql`, `catalog/db.py`, `tests/__init__.py`, `tests/test_db.py`

**Interfaces:**
- Produces: `config.DB_PATH/IMG_DIR/AGENT_*`；`db.connect()->sqlite3.Connection`（row_factory=Row）；`db.init_db(conn)`；schema 表：`product_razor/product_curler/embedding/approval_ticket/import_doc`

- [x] **Step 1: 写失败测试**

```python
# tests/test_db.py
import sqlite3
from catalog import db

def test_init_db_creates_category_tables_with_template_columns():
    conn = sqlite3.connect(':memory:')
    conn.row_factory = sqlite3.Row
    db.init_db(conn)
    for table in ('product_razor', 'product_curler', 'embedding',
                  'approval_ticket', 'import_doc'):
        names = [r['name'] for r in conn.execute(f'PRAGMA table_info({table})')]
        assert names, f'{table} 缺失'
    razor = [r['name'] for r in conn.execute('PRAGMA table_info(product_razor)')]
    assert 'model_no' in razor and 'giftbox_mm' in razor and 'inner_code' in razor
    curler = [r['name'] for r in conn.execute('PRAGMA table_info(product_curler)')]
    assert 'item_no' in curler and 'heater' in curler and 'frequency' in curler
```

- [x] **Step 2: 跑测试确认失败**

Run: `cd /Users/elias/code/eryuan/dangkou-v2 && python3 -m pytest tests/test_db.py -v`
Expected: FAIL（ModuleNotFoundError: catalog）

- [x] **Step 3: 最小实现**

```python
# catalog/config.py
import os
_DB = os.path.join(os.path.dirname(__file__), '..', '.env')
try:
    for line in open(_DB, encoding='utf-8'):
        line = line.strip()
        if line and not line.startswith('#') and '=' in line:
            k, v = line.split('=', 1)
            os.environ.setdefault(k.strip(), v.strip())
except FileNotFoundError:
    pass

DB_PATH = os.environ.get('CATALOG_V2_DB',
                         os.path.join(os.path.dirname(__file__), '..', 'data', 'catalog.db'))
IMG_DIR = os.environ.get('CATALOG_V2_IMG',
                         os.path.join(os.path.dirname(__file__), '..', 'data', 'images'))
SERVICE_TOKEN = os.environ.get('CATALOG_V2_SERVICE_TOKEN', '')
AGENT_BASE_URL = os.environ.get('CATALOG_AGENT_BASE_URL', 'https://ccdox.0755.click')
AGENT_API_KEY = os.environ.get('CATALOG_AGENT_API_KEY', '')
AGENT_MODEL = os.environ.get('CATALOG_AGENT_MODEL', 'claude-sonnet-5')
AGENT_TIMEOUT = int(os.environ.get('CATALOG_AGENT_TIMEOUT', '2700'))
BAILIAN_API_KEY = os.environ.get('BAILIAN_API_KEY', '')
BAILIAN_BASE_URL = os.environ.get(
    'BAILIAN_BASE_URL',
    'https://ws-gox0m0606t27jojm.cn-beijing.maas.aliyuncs.com/compatible-mode/v1')
```

```sql
-- catalog/schema.sql
CREATE TABLE IF NOT EXISTS product_razor (
  id TEXT PRIMARY KEY, inner_code TEXT UNIQUE NOT NULL,
  model_no TEXT, description TEXT, color TEXT, size_mm TEXT,
  giftbox_mm TEXT, unit_weight_g TEXT, ctn_spec TEXT, price TEXT,
  status TEXT NOT NULL DEFAULT 'approved',
  image_main TEXT, images TEXT DEFAULT '[]',
  source_doc INTEGER, created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_razor_model ON product_razor(model_no);
CREATE TABLE IF NOT EXISTS product_curler (
  id TEXT PRIMARY KEY, inner_code TEXT UNIQUE NOT NULL,
  item_no TEXT, ctn_size TEXT, ctn_qty TEXT, price TEXT, voltage TEXT,
  power TEXT, heater TEXT, material TEXT, frequency TEXT,
  status TEXT NOT NULL DEFAULT 'approved',
  image_main TEXT, images TEXT DEFAULT '[]',
  source_doc INTEGER, created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_curler_item ON product_curler(item_no);
CREATE TABLE IF NOT EXISTS embedding (
  product_id TEXT NOT NULL, category TEXT NOT NULL,
  image_path TEXT NOT NULL, vec BLOB NOT NULL,
  text_vec BLOB,
  PRIMARY KEY (product_id, image_path)
);
CREATE TABLE IF NOT EXISTS approval_ticket (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  ticket_type TEXT NOT NULL,          -- import | mutate
  category TEXT, payload TEXT NOT NULL,-- JSON：草稿行/改前改后
  status TEXT NOT NULL DEFAULT 'pending',  -- pending | approved | rejected
  token TEXT UNIQUE NOT NULL, token_used_at TEXT,
  created_at TEXT DEFAULT (datetime('now')), decided_at TEXT
);
CREATE TABLE IF NOT EXISTS import_doc (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  filename TEXT NOT NULL, category TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'parsing',  -- parsing|ticketed|failed
  stats_json TEXT, error TEXT,
  created_at TEXT DEFAULT (datetime('now'))
);
```

```python
# catalog/db.py
import os
import sqlite3

_SCHEMA = os.path.join(os.path.dirname(__file__), 'schema.sql')

def connect(path=None):
    from . import config
    p = path or config.DB_PATH
    os.makedirs(os.path.dirname(p), exist_ok=True)
    conn = sqlite3.connect(p)
    conn.row_factory = sqlite3.Row
    conn.execute('PRAGMA journal_mode=WAL')
    return conn

def init_db(conn):
    conn.executescript(open(_SCHEMA, encoding='utf-8').read())
    conn.commit()
```

```text
# requirements.txt
fastapi
uvicorn
openpyxl
requests
pytest
python-multipart
```

```text
# .env.example
CATALOG_V2_SERVICE_TOKEN=
CATALOG_AGENT_BASE_URL=https://ccdox.0755.click
CATALOG_AGENT_API_KEY=
CATALOG_AGENT_MODEL=claude-sonnet-5
BAILIAN_API_KEY=
BAILIAN_BASE_URL=https://ws-gox0m0606t27jojm.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
```

- [x] **Step 4: 跑测试确认通过**

Run: `python3 -m pytest tests/test_db.py -v`
Expected: PASS

- [x] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: 骨架+配置+品类物理表schema"
```

### Task 2: 模板常量（唯一事实源）+ 行映射

**Files:**
- Create: `catalog/templates.py`, `tests/test_templates.py`

**Interfaces:**
- Produces: `templates.TEMPLATES: dict[key->Template]`；`Template` 字段：`key/name/table/dedup_field/fields`（fields= `(col,label)` 有序列表）；`templates.common_cols()`；`templates.row_to_dict(template, row)->dict`（含 inner_code/status/image_main 等）

- [x] **Step 1: 写失败测试**

```python
# tests/test_templates.py
from catalog import templates

def test_two_categories_with_dedup_keys():
    assert set(templates.TEMPLATES) == {'razor', 'curler'}
    assert templates.TEMPLATES['razor'].dedup_field == 'model_no'
    assert templates.TEMPLATES['curler'].dedup_field == 'item_no'

def test_field_labels_match_prd():
    razor_labels = [l for _, l in templates.TEMPLATES['razor'].fields]
    assert '产品型号' in razor_labels and '彩盒尺寸(mm)' in razor_labels
    curler_labels = [l for _, l in templates.TEMPLATES['curler'].fields]
    assert '发热体' in curler_labels and '频率' in curler_labels

def test_row_to_dict_maps_template_fields():
    t = templates.TEMPLATES['razor']
    row = {'id': 'x', 'inner_code': 'KS-AAAAAAAA', 'model_no': '8225',
           'color': '黑', 'status': 'approved', 'image_main': 'a.png'}
    d = templates.row_to_dict(t, row)
    assert d['产品型号'] == '8225' and d['颜色'] == '黑' and d['内部货号'] == 'KS-AAAAAAAA'
```

- [x] **Step 2: 跑测试确认失败**

Run: `python3 -m pytest tests/test_templates.py -v` → FAIL

- [x] **Step 3: 最小实现**

```python
# catalog/templates.py
from dataclasses import dataclass

@dataclass(frozen=True)
class Template:
    key: str; name: str; table: str; dedup_field: str
    fields: tuple  # ((col, label), ...) 有序=列序

RAZOR = Template('razor', '剃须刀', 'product_razor', 'model_no', (
    ('model_no', '产品型号'), ('description', '功能描述'), ('color', '颜色'),
    ('size_mm', '产品尺寸(mm)'), ('giftbox_mm', '彩盒尺寸(mm)'),
    ('unit_weight_g', '单套重量(g)'), ('ctn_spec', '箱规'), ('price', '报价'),
))
CURLER = Template('curler', '卷发棒', 'product_curler', 'item_no', (
    ('item_no', 'ITEM.NO 型号'), ('ctn_size', '装箱尺寸'), ('ctn_qty', '装箱数量'),
    ('price', '价格'), ('voltage', '电压'), ('power', '功率'),
    ('heater', '发热体'), ('material', '材质'), ('frequency', '频率'),
))
TEMPLATES = {t.key: t for t in (RAZOR, CURLER)}

COMMON = (('inner_code', '内部货号'),)  # 展示用公共列（入库列见 schema.sql）

def row_to_dict(t: Template, row) -> dict:
    d = {label: row[col] for col, label in t.fields}
    d.update({'内部货号': row['inner_code'], '状态': row['status'],
              '主图': row['image_main'], 'id': row['id']})
    return d
```

- [x] **Step 4: 跑测试通过** → `- [ ]` Run: `python3 -m pytest tests/test_templates.py -v` → PASS

- [x] **Step 5: Commit** `git add -A && git commit -m "feat: 品类模板常量（唯一事实源）"`

### Task 3: 内部货号生成

**Files:**
- Create: `catalog/inner_code.py`, `tests/test_inner_code.py`

**Interfaces:**
- Produces: `inner_code.gen() -> str`（KS-8位）

- [x] **Step 1: 失败测试**

```python
# tests/test_inner_code.py
import re
from catalog import inner_code

def test_format_and_alphabet_and_uniqueness():
    seen = set()
    for _ in range(500):
        c = inner_code.gen()
        assert re.fullmatch(r'KS-[ABCDEFGHJKLMNPQRSTUVWXYZ23456789]{8}', c)
        seen.add(c)
    assert len(seen) >= 495  # 500 次碰撞极少
```

- [x] **Step 2: 确认失败** → Run 失败

- [x] **Step 3: 实现**（搬旧仓库 common.py 同款规则）

```python
# catalog/inner_code.py
import secrets

_ALPHABET = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'  # 无 0/O/1/I

def gen() -> str:
    return 'KS-' + ''.join(secrets.choice(_ALPHABET) for _ in range(8))
```

- [x] **Step 4: 通过** → `- [ ]` Run → PASS

- [x] **Step 5: Commit** `git commit -m "feat: 内部货号 KS-XXXXXXXX"`

### Task 4: 图片本地存储（接口抽象）

**Files:**
- Create: `catalog/storage.py`, `tests/test_storage.py`

**Interfaces:**
- Produces: `storage.LocalStorage(base_dir)`：`.save(category, product_id, filename, data) -> rel`；`.abs_path(rel) -> str`；`.read(rel) -> bytes`。rel 形如 `razor/<id>/main.png`（后续 OSS 实现同签名替换）

- [x] **Step 1: 失败测试**

```python
# tests/test_storage.py
import os
from catalog.storage import LocalStorage

def test_save_and_read_roundtrip(tmp_path):
    s = LocalStorage(str(tmp_path))
    rel = s.save('razor', 'id1', 'main.png', b'PNGDATA')
    assert rel == os.path.join('razor', 'id1', 'main.png')
    assert open(s.abs_path(rel), 'rb').read() == b'PNGDATA'
    assert s.read(rel) == b'PNGDATA'
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**

```python
# catalog/storage.py
import os

class LocalStorage:
    """图片存储·本地实现。OSS 实现时同签名替换（save/abs_path/read）。"""
    def __init__(self, base_dir):
        self.base = base_dir

    def save(self, category, product_id, filename, data: bytes) -> str:
        rel = os.path.join(category, product_id, filename)
        dest = os.path.join(self.base, rel)
        os.makedirs(os.path.dirname(dest), exist_ok=True)
        with open(dest, 'wb') as f:
            f.write(data)
        return rel

    def abs_path(self, rel) -> str:
        return os.path.join(self.base, rel)

    def read(self, rel) -> bytes:
        return open(self.abs_path(rel), 'rb').read()
```

- [x] **Step 4: 通过**

- [x] **Step 5: Commit** `git commit -m "feat: 图片存储接口+本地实现"`

---

## M2 导入管线

### Task 5: API 骨架 + 图片鉴权服务

**Files:**
- Create: `catalog/api.py`, `catalog/main.py`, `tests/test_api.py`

**Interfaces:**
- Consumes: `db.connect/init_db`，`config.SERVICE_TOKEN`，`storage.LocalStorage`
- Produces: FastAPI app（`main.app`）；路由 `GET /health`、`GET /img/{rel:path}`（`X-Service-Token` 或 `?token=` 鉴权）；后续任务往此 app 加路由

- [x] **Step 1: 失败测试**

```python
# tests/test_api.py
import sqlite3
import pytest
from fastapi.testclient import TestClient
from catalog import db
from catalog.main import app, storage

@pytest.fixture()
def client(tmp_path, monkeypatch):
    monkeypatch.setattr('catalog.main.storage', type('S', (), {
        'base': str(tmp_path)})() and __import__('catalog.storage', fromlist=['LocalStorage']).LocalStorage(str(tmp_path)))
    conn = sqlite3.connect(':memory:'); conn.row_factory = sqlite3.Row
    db.init_db(conn)
    app.state.conn = conn
    app.state.token = 'T0KEN'
    return TestClient(app)

def test_health(client):
    r = client.get('/health')
    assert r.status_code == 200 and r.json()['status'] == 'ready'

def test_img_requires_token(client, tmp_path):
    open(tmp_path / 'razor' / 'id1' / 'main.png', 'wb').write(b'x') if False else None
    from catalog.storage import LocalStorage
    s = LocalStorage(str(tmp_path)); s.save('razor', 'id1', 'main.png', b'PNG')
    assert client.get('/img/razor/id1/main.png').status_code == 401
    r = client.get('/img/razor/id1/main.png', headers={'X-Service-Token': 'T0KEN'})
    assert r.status_code == 200 and r.content == b'PNG'
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**

```python
# catalog/api.py
import sqlite3
from fastapi import FastAPI, HTTPException, Request, Response

def _auth(request: Request, token: str):
    if not token:
        return
    h = request.headers.get('X-Service-Token')
    if h != token and request.query_params.get('token') != token:
        raise HTTPException(401, 'unauthorized')

def register_routes(app: FastAPI):
    @app.get('/health')
    def health():
        return {'status': 'ready', 'version': 'v2.0'}

    @app.get('/img/{rel:path}')
    def img(rel: str, request: Request):
        _auth(request, app.state.token)
        try:
            data = app.state.storage.read(rel)
        except FileNotFoundError:
            raise HTTPException(404, 'no image')
        return Response(content=data, media_type='image/png')
```

```python
# catalog/main.py
import os
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from . import config, db
from .api import register_routes
from .storage import LocalStorage

app = FastAPI(title='dangkou catalog v2')
app.state.conn = db.connect()
db.init_db(app.state.conn)
app.state.token = config.SERVICE_TOKEN
app.state.storage = LocalStorage(config.IMG_DIR)
register_routes(app)
_static = os.path.join(os.path.dirname(__file__), '..', 'static')
if os.path.isdir(_static):
    app.mount('/', StaticFiles(directory=_static, html=True), name='static')
```

注意：`/img` 路由需在 StaticFiles 挂载前注册（FastAPI 按注册顺序匹配），`register_routes(app)` 在 mount 之前调用即满足。

- [x] **Step 4: 通过** → `python3 -m pytest tests/test_api.py -v`

- [x] **Step 5: Commit** `git commit -m "feat: API骨架+图片鉴权服务"`

### Task 6: Agent 解析（模板字段提示词 + spawn + 超时拯救）

**Files:**
- Create: `catalog/agent.py`, `tests/test_agent.py`, `tests/fake_claude.sh`

**Interfaces:**
- Consumes: `templates.TEMPLATES`，`config.AGENT_*`
- Produces: `agent.build_prompt(template_key, xlsx_path, out_json) -> str`；`agent.parse(template_key, xlsx_path, out_dir) -> dict`（`{'vendor','products':[...]}`，product 键=模板 col 名）。超时但产物已写出→收用

- [x] **Step 1: 失败测试**（用 fake claude 脚本，不调真模型）

```python
# tests/test_agent.py
import json
import os
import stat
import textwrap
from catalog import agent, templates

def _make_fake(tmp_path, out_json, products, hang=False):
    fake = tmp_path / 'claude'
    body = textwrap.dedent(f'''
        #!/bin/bash
        mkdir -p "$(dirname "{out_json}")"
        cat > "{out_json}" << 'EOF'
        {{"vendor": "测试厂", "products": {json.dumps(products, ensure_ascii=False)}}}
        EOF
        echo '{{"type":"result","subtype":"success","result":"DONE 1"}}'
    ''').strip()
    if hang:
        body = '#!/bin/bash\nsleep 600'
    fake.write_text(body)
    fake.chmod(fake.stat().st_mode | stat.S_IEXEC)
    return str(fake)

def test_build_prompt_contains_template_fields_and_rules():
    p = agent.build_prompt('razor', '/tmp/in.xlsx', '/tmp/out.json')
    for label in ('产品型号', '彩盒尺寸(mm)', '报价'):
        assert label in p
    assert '同型号' in p and '内部货号' not in p.split('基本规则')[0]

def test_parse_returns_products_via_fake_agent(tmp_path, monkeypatch):
    out = str(tmp_path / 'products.json')
    monkeypatch.setattr(agent.shutil, 'which', lambda _: _make_fake(
        tmp_path, out, [{'model_no': '8225', 'price': '21.5'}]))
    monkeypatch.setattr(agent.time, 'sleep', lambda s: None)
    r = agent.parse('razor', str(tmp_path / 'in.xlsx'), str(tmp_path))
    assert r['vendor'] == '测试厂'
    assert r['products'][0]['model_no'] == '8225'
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**（提示词沿用实战验证的硬约束方法论；搬运旧仓库 agent_claude.py v5 结构）

```python
# catalog/agent.py
import json
import os
import shutil
import subprocess
import time

from . import config
from .templates import TEMPLATES

PROMPT = '''解析这份 Excel 厂家报价单，按品类模板产出商品库。

输入：__FILE__（你的工作目录）
输出：__OUT__
品类：__CAT_NAME__，模板字段（列名=键名，逐字段填，原文有就填没有留空）：__FIELDS__

# 硬性验收标准
同产品型号绝不能出现两条——同型号多行合并为一条，颜色/规格变体差异值用 / 连接；
纵向合并单元格的从属行必须继承组首行值归入同一商品。
内嵌图片在 xlsx（zip）的 xl/media/、锚点在 xl/drawings/：解到工作目录（r行号_c列号.扩展名），
每条商品 image_main 填对应文件名、image_count 填数量，一货一图。
定稿前自检：无重复型号、商品数合理（通常≈行数减表头），并抽 5-10 行核对归属。

# 输出
{"vendor":"厂家名或null","products":[{...模板字段...,"image_main":"...","image_count":N}]}
python json.dump(..., ensure_ascii=False, indent=1) 写入输出文件。
完成后只回一行：DONE N（N=商品数）'''


def build_prompt(template_key, xlsx_path, out_json) -> str:
    t = TEMPLATES[template_key]
    fields = ', '.join(f'{label}({col})' for col, label in t.fields)
    return (PROMPT.replace('__FILE__', xlsx_path)
            .replace('__OUT__', out_json)
            .replace('__CAT_NAME__', t.name)
            .replace('__FIELDS__', fields))


def parse(template_key, xlsx_path, work_dir) -> dict:
    claude = shutil.which('claude')
    if not claude:
        raise RuntimeError('claude CLI 不在 PATH')
    out_json = os.path.join(work_dir, 'products.json')
    if os.path.exists(out_json):
        os.remove(out_json)
    prompt = build_prompt(template_key, xlsx_path, out_json)
    env = os.environ.copy()
    env['ANTHROPIC_BASE_URL'] = config.AGENT_BASE_URL
    env['ANTHROPIC_API_KEY'] = config.AGENT_API_KEY
    timed_out, r = False, None
    try:
        r = subprocess.run(
            [claude, '-p', prompt, '--output-format', 'json',
             '--dangerously-skip-permissions', '--model', config.AGENT_MODEL],
            capture_output=True, text=True, timeout=config.AGENT_TIMEOUT,
            env=env, cwd=work_dir)
    except subprocess.TimeoutExpired:
        timed_out = True   # 超时拯救：产物已写出则收用
    if not os.path.exists(out_json):
        raise RuntimeError(f'Agent 未产出结果文件(timed_out={timed_out})')
    if timed_out:
        print('[agent] 超时拯救：收用已写出的结果文件', flush=True)
    data = json.load(open(out_json))
    # 防御性去重（同型号保留字段更全的）
    t = TEMPLATES[template_key]
    best = {}
    for p in data.get('products', []):
        k = str(p.get(t.dedup_field, '')).strip()
        if not k:
            continue
        if k not in best or len(json.dumps(p, ensure_ascii=False)) > len(json.dumps(best[k], ensure_ascii=False)):
            best[k] = p
    data['products'] = list(best.values())
    return data
```

- [x] **Step 4: 通过** → `python3 -m pytest tests/test_agent.py -v`

- [x] **Step 5: Commit** `git commit -m "feat: Agent解析（模板提示词+超时拯救+防御去重）"`

### Task 7: 导入四分类

**Files:**
- Create: `catalog/classify.py`, `tests/test_classify.py`

**Interfaces:**
- Consumes: `templates.TEMPLATES`
- Produces: `classify.classify(template_key, drafts, existing_rows) -> dict`：`{'new': [draft...], 'update': [(row, draft)...], 'delist': [row...]}`；draft=模板 col 键的 dict。匹配键=`template.dedup_field`

- [x] **Step 1: 失败测试**

```python
# tests/test_classify.py
from catalog import classify

def test_four_way_classification():
    existing = [
        {'id': 'a', 'model_no': '8225', 'price': '20'},
        {'id': 'b', 'model_no': 'OLD1', 'price': '9'},
    ]
    drafts = [
        {'model_no': '8225', 'price': '23'},        # 改值=更新
        {'model_no': '8226', 'price': '25'},        # 新型号=新增
    ]
    r = classify.classify('razor', drafts, existing)
    assert len(r['new']) == 1 and r['new'][0]['model_no'] == '8226'
    assert len(r['update']) == 1 and r['update'][0][0]['id'] == 'a'
    assert r['update'][0][1]['price'] == '23'
    assert len(r['delist']) == 1 and r['delist'][0]['id'] == 'b'  # 删行=下架
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**

```python
# catalog/classify.py
from .templates import TEMPLATES

def classify(template_key, drafts, existing_rows):
    t = TEMPLATES[template_key]
    key = t.dedup_field
    existing = {str(r[key]).strip(): r for r in existing_rows if str(r[key] or '').strip()}
    incoming = {str(d.get(key, '')).strip(): d for d in drafts if str(d.get(key, '') or '').strip()}
    new = [d for k, d in incoming.items() if k not in existing]
    update = [(existing[k], d) for k, d in incoming.items()
              if k in existing and _differs(existing[k], d, t)]
    delist = [r for k, r in existing.items() if k not in incoming]
    return {'new': new, 'update': update, 'delist': delist}

def _differs(row, draft, t) -> bool:
    return any(str(row[col] or '') != str(draft.get(col) or '') for col, _ in t.fields)
```

- [x] **Step 4: 通过**

- [x] **Step 5: Commit** `git commit -m "feat: 导入四分类（新增/更新/下架）"`

### Task 8: 审批工单（一次性 token）

**Files:**
- Create: `catalog/tickets.py`, `tests/test_tickets.py`

**Interfaces:**
- Consumes: `db.connect`
- Produces: `tickets.create(conn, ticket_type, category, payload) -> {'id','token'}`；`tickets.decide(conn, ticket_id, token, approved, decisions=None) -> dict`（执行 payload 落库；token 二次用报错）；payload 格式：import=`{'drafts':{'new':...,'update':...,'delist':[id...]}}`，mutate=`{'action':'update'|'delete'|'create','product_id','changes':{col:val}}`

- [x] **Step 1: 失败测试**

```python
# tests/test_tickets.py
import sqlite3
import pytest
from catalog import db, tickets

@pytest.fixture()
def conn():
    c = sqlite3.connect(':memory:'); c.row_factory = sqlite3.Row
    db.init_db(c)
    return c

def test_token_is_one_time(conn):
    t = tickets.create(conn, 'import', 'razor',
                       {'drafts': {'new': [{'model_no': '8225'}],
                                   'update': [], 'delist': []}})
    r = tickets.decide(conn, t['id'], t['token'], approved=True)
    assert r['created'] == 1
    with pytest.raises(tickets.TicketError):
        tickets.decide(conn, t['id'], t['token'], approved=True)  # 已用
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**

```python
# catalog/tickets.py
import json
import secrets
import sqlite3

from . import inner_code
from .templates import TEMPLATES

class TicketError(Exception):
    pass

def create(conn, ticket_type, category, payload) -> dict:
    token = secrets.token_urlsafe(24)
    cur = conn.execute(
        'INSERT INTO approval_ticket(ticket_type, category, payload, token) VALUES(?,?,?,?)',
        (ticket_type, category, json.dumps(payload, ensure_ascii=False), token))
    conn.commit()
    return {'id': cur.lastrowid, 'token': token}

def decide(conn, ticket_id, token, approved: bool, decisions=None) -> dict:
    row = conn.execute('SELECT * FROM approval_ticket WHERE id=?', (ticket_id,)).fetchone()
    if row is None:
        raise TicketError('工单不存在')
    if row['token_used_at'] is not None:
        raise TicketError('token 已使用（一次性）')
    if row['token'] != token or row['status'] != 'pending':
        raise TicketError('token 无效或工单已决')
    if not approved:
        conn.execute("UPDATE approval_ticket SET status='rejected', token_used_at=datetime('now'), "
                     "decided_at=datetime('now') WHERE id=?", (ticket_id,))
        conn.commit()
        return {'rejected': True}
    result = _apply(conn, json.loads(row['payload']), row['category'], decisions)
    conn.execute("UPDATE approval_ticket SET status='approved', token_used_at=datetime('now'), "
                 "decided_at=datetime('now') WHERE id=?", (ticket_id,))
    conn.commit()
    return result

def _apply(conn, payload, category, decisions) -> dict:
    t = TEMPLATES[category]
    if payload.get('kind') == 'mutate':
        return _apply_mutate(conn, t, payload)
    drafts = payload['drafts']
    rejected_ids = set((decisions or {}).get('reject', []))
    cols = [c for c, _ in t.fields]

    def _keep(d):
        pid = d.get('id') if isinstance(d, dict) else d
        return pid not in rejected_ids

    created, created_rows = 0, []
    for d in drafts.get('new', []):
        if not _keep(d):
            continue
        cols_vals = [(c, str(d.get(c, '') or '')) for c, _ in t.fields]
        pid = secrets.token_hex(8)
        conn.execute(
            f"INSERT INTO {t.table}(id, inner_code, {', '.join(c for c, _ in cols_vals)}, "
            f"image_main, images, source_doc) VALUES(?,?,?,?,?,?)",
            (pid, inner_code.gen(),
             *[v for _, v in cols_vals],
             d.get('image_main') or '', json.dumps(d.get('images') or [], ensure_ascii=False),
             payload.get('doc_id')))
        created_rows.append({'id': pid, 'image_main': d.get('image_main') or ''})
        created += 1
    updated = 0
    for row, d in drafts.get('update', []):
        if not _keep(d):
            continue
        sets = ', '.join(f"{c}=?" for c, _ in t.fields)
        conn.execute(f"UPDATE {t.table} SET {sets}, updated_at=datetime('now') WHERE id=?",
                     (*[str(d.get(c, '') or '') for c, _ in t.fields], row['id']))
        updated += 1
    delisted = 0
    for rid in drafts.get('delist', []):
        conn.execute(f"UPDATE {t.table} SET status='delisted', "
                     f"updated_at=datetime('now') WHERE id=?", (rid['id'] if isinstance(rid, dict) else rid,))
        delisted += 1
    return {'created': created, 'updated': updated, 'delisted': delisted,
            'created_rows': created_rows, 'work_dir': payload.get('work_dir')}

def _apply_mutate(conn, t, payload) -> dict:
    action = payload['action']
    if action == 'update':
        sets = ', '.join(f"{c}=?" for c in payload['changes'])
        conn.execute(f"UPDATE {t.table} SET {sets}, updated_at=datetime('now') WHERE id=?",
                     (*payload['changes'].values(), payload['product_id']))
    elif action == 'delete':
        conn.execute(f"UPDATE {t.table} SET status='delisted', "
                     f"updated_at=datetime('now') WHERE id=?", (payload['product_id'],))
    elif action == 'create':
        cols = list(payload['changes'])
        conn.execute(f"INSERT INTO {t.table}(id, inner_code, {', '.join(cols)}) "
                     f"VALUES(?,?,{','.join('?' for _ in cols)})",
                     (secrets.token_hex(8), inner_code.gen(), *payload['changes'].values()))
    return {'mutated': action}
```

- [x] **Step 4: 通过**

- [x] **Step 5: Commit** `git commit -m "feat: 审批工单+一次性token+落库执行"`

### Task 9: 异步导入编排 + 回调

**Files:**
- Create: `catalog/ingest.py`, `tests/test_ingest.py`

**Interfaces:**
- Consumes: `agent.parse`，`classify.classify`，`tickets.create`，`db`
- Produces: `ingest.start(conn, storage, xlsx_path, category, callback=None) -> doc_id`（立即返回；后台线程：解析→四分类→建 import 工单→改 doc 状态→`callback(doc_id, ticket_id, token, stats)`）；`ingest.status(conn, doc_id) -> dict`

- [x] **Step 1: 失败测试**（agent.parse 打桩）

```python
# tests/test_ingest.py
import sqlite3
import time
import pytest
from catalog import db, ingest

@pytest.fixture()
def conn():
    c = sqlite3.connect(':memory:'); c.row_factory = sqlite3.Row
    db.init_db(c)
    return c

def _wait_done(conn, doc_id, timeout=10):
    for _ in range(timeout * 10):
        s = ingest.status(conn, doc_id)
        if s['status'] in ('ticketed', 'failed'):
            return s
        time.sleep(0.1)
    raise AssertionError('超时未完成')

def test_async_import_creates_ticket_and_calls_back(conn, monkeypatch, tmp_path):
    calls = []
    monkeypatch.setattr(ingest.agent, 'parse', lambda cat, p, wd: {
        'vendor': '厂', 'products': [{'model_no': '8225', 'price': '21.5'}]})
    from catalog.storage import LocalStorage
    st = LocalStorage(str(tmp_path))
    doc_id = ingest.start(conn, st, str(tmp_path / 'f.xlsx'), 'razor',
                          callback=lambda **kw: calls.append(kw))
    s = _wait_done(conn, doc_id)
    assert s['status'] == 'ticketed'
    assert calls and calls[0]['stats']['new'] == 1
    tk = conn.execute('SELECT * FROM approval_ticket').fetchone()
    assert tk['ticket_type'] == 'import' and tk['category'] == 'razor'

def test_failed_parse_marks_doc_failed(conn, monkeypatch, tmp_path):
    def boom(cat, p, wd):
        raise RuntimeError('解析炸了')
    monkeypatch.setattr(ingest.agent, 'parse', boom)
    from catalog.storage import LocalStorage
    doc_id = ingest.start(conn, LocalStorage(str(tmp_path)), str(tmp_path / 'f.xlsx'), 'razor')
    s = _wait_done(conn, doc_id)
    assert s['status'] == 'failed' and '解析炸了' in s['error']
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**

```python
# catalog/ingest.py
import json
import os
import threading
import traceback
import uuid

from . import agent, classify, tickets
from .templates import TEMPLATES

def start(conn, storage, xlsx_path, category, callback=None) -> int:
    if category not in TEMPLATES:
        raise ValueError(f'未知品类: {category}')
    cur = conn.execute(
        'INSERT INTO import_doc(filename, category, status) VALUES(?,?,?)',
        (os.path.basename(xlsx_path), category, 'parsing'))
    conn.commit()
    doc_id = cur.lastrowid

    def _bg():
        try:
            work_dir = os.path.join(storage.base, '_work', f'doc{doc_id}-{uuid.uuid4().hex[:6]}')
            os.makedirs(work_dir, exist_ok=True)
            result = agent.parse(category, xlsx_path, work_dir)
            t = TEMPLATES[category]
            existing = conn.execute(
                f'SELECT * FROM {t.table} WHERE status != "delisted"').fetchall()
            c = classify.classify(category, result['products'], existing)
            payload = {'kind': 'import', 'doc_id': doc_id, 'work_dir': work_dir,
                       'drafts': {'new': c['new'],
                                  'update': [[dict(r), d] for r, d in c['update']],
                                  'delist': [dict(r) for r in c['delist']]}}
            tk = tickets.create(conn, 'import', category, payload)
            stats = {'new': len(c['new']), 'update': len(c['update']),
                     'delist': len(c['delist']), 'vendor': result.get('vendor')}
            conn.execute("UPDATE import_doc SET status='ticketed', stats_json=? WHERE id=?",
                         (json.dumps(stats, ensure_ascii=False), doc_id))
            conn.commit()
            if callback:
                callback(doc_id=doc_id, ticket_id=tk['id'], token=tk['token'], stats=stats)
        except Exception as e:  # noqa: BLE001
            conn.execute("UPDATE import_doc SET status='failed', error=? WHERE id=?",
                         (str(e)[:300], doc_id))
            conn.commit()
            traceback.print_exc()
            if callback:
                callback(doc_id=doc_id, ticket_id=None, token=None,
                         stats={'error': str(e)[:200]})

    threading.Thread(target=_bg, daemon=True).start()
    return doc_id

def status(conn, doc_id) -> dict:
    r = conn.execute('SELECT * FROM import_doc WHERE id=?', (doc_id,)).fetchone()
    if r is None:
        raise KeyError(doc_id)
    return {'id': r['id'], 'status': r['status'], 'stats': json.loads(r['stats_json'] or 'null'),
            'error': r['error']}
```

- [x] **Step 4: 通过**

- [x] **Step 5: Commit** `git commit -m "feat: 异步导入编排+回调+工单串联"`

---

## M3 管理界面

### Task 10: 商品 CRUD API（按品类）+ 导入/工单路由

**Files:**
- Modify: `catalog/api.py`
- Test: `tests/test_crud_api.py`

**Interfaces:**
- Consumes: `ingest.start/status`，`tickets.decide`，`templates.TEMPLATES`
- Produces: 路由 `POST /import`（body `{path, category}` → `{doc_id}`）；`GET /import/{id}`；`GET /tickets`、`POST /tickets/{id}/decision`（body `{token, approved, decisions}`）；`GET /products/{category}`（返回 `[row_to_dict...]` + 模板元信息）；`POST /products/{category}`（AI/用户建 → 直接建**审批工单** kind=mutate）；`PATCH /products/{category}/{id}`（同上，update 工单）；`DELETE /products/{category}/{id}`（delist 工单）

- [x] **Step 1: 失败测试**

```python
# tests/test_crud_api.py
import sqlite3
import pytest
from fastapi.testclient import TestClient
from catalog import db, ingest
from catalog.main import app

@pytest.fixture()
def client(tmp_path, monkeypatch):
    monkeypatch.setattr(ingest.agent, 'parse', lambda cat, p, wd: {
        'vendor': '厂', 'products': [{'model_no': '8225', 'price': '21.5'}]})
    conn = sqlite3.connect(':memory:'); conn.row_factory = sqlite3.Row
    db.init_db(conn)
    app.state.conn = conn; app.state.token = ''
    from catalog.storage import LocalStorage
    monkeypatch.setattr('catalog.main.storage', LocalStorage(str(tmp_path)))
    app.state.storage = LocalStorage(str(tmp_path))
    return TestClient(app)

def _import_and_approve(client, tmp_path):
    f = tmp_path / 'f.xlsx'; f.write_bytes(b'x')
    doc = client.post('/import', json={'path': str(f), 'category': 'razor'}).json()['doc_id']
    tk = client.get('/tickets').json()['tickets'][0]
    client.post(f"/tickets/{tk['id']}/decision",
                json={'token': tk['token'], 'approved': True})
    return doc

def test_import_flow_to_products(client, tmp_path):
    _import_and_approve(client, tmp_path)
    r = client.get('/products/razor').json()
    assert r['template']['name'] == '剃须刀'
    assert len(r['products']) == 1 and r['products'][0]['产品型号'] == '8225'

def test_patch_creates_mutate_ticket_not_direct_write(client, tmp_path):
    _import_and_approve(client, tmp_path)
    pid = client.get('/products/razor').json()['products'][0]['id']
    r = client.patch(f'/products/razor/{pid}', json={'changes': {'price': '23'}})
    assert r.json()['ticket_id']
    assert client.get('/products/razor').json()['products'][0]['报价'] == '21.5'  # 未批不改
    tks = client.get('/tickets').json()['tickets']
    mt = [t for t in tks if t['ticket_type'] == 'mutate'][0]
    client.post(f"/tickets/{mt['id']}/decision", json={'token': mt['token'], 'approved': True})
    assert client.get('/products/razor').json()['products'][0]['报价'] == '23'
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**（追加到 `register_routes`）

```python
    # —— 追加在 register_routes 内 ——
    from . import ingest, tickets
    from .templates import TEMPLATES, row_to_dict
    from pydantic import BaseModel

    class ImportIn(BaseModel):
        path: str; category: str

    @app.post('/import')
    def do_import(body: ImportIn, request: Request):
        _auth(request, app.state.token)
        import os
        if not os.path.isfile(body.path):
            raise HTTPException(404, f'文件不存在: {body.path}')
        return {'doc_id': ingest.start(app.state.conn, app.state.storage,
                                       body.path, body.category,
                                       callback=app.state.callback)}

    @app.get('/import/{doc_id}')
    def import_status(doc_id: int):
        return ingest.status(app.state.conn, doc_id)

    @app.get('/tickets')
    def list_tickets():
        rows = app.state.conn.execute(
            'SELECT id, ticket_type, category, status, created_at FROM approval_ticket '
            'ORDER BY id DESC').fetchall()
        return {'tickets': [dict(r) for r in rows]}

    class DecisionIn(BaseModel):
        token: str; approved: bool; decisions: dict | None = None

    @app.post('/tickets/{ticket_id}/decision')
    def decide_ticket(ticket_id: int, body: DecisionIn):
        try:
            return tickets.decide(app.state.conn, ticket_id, body.token,
                                  body.approved, body.decisions)
        except tickets.TicketError as e:
            raise HTTPException(400, str(e))

    @app.get('/products/{category}')
    def list_products(category: str):
        t = TEMPLATES[category]
        rows = app.state.conn.execute(
            f'SELECT * FROM {t.table} ORDER BY id').fetchall()
        return {'template': {'key': t.key, 'name': t.name,
                             'fields': [{'col': c, 'label': l} for c, l in t.fields]},
                'products': [row_to_dict(t, r) for r in rows]}

    class MutateIn(BaseModel):
        changes: dict; product_id: str | None = None

    @app.post('/products/{category}')
    def create_product(category: str, body: MutateIn, request: Request):
        _auth(request, app.state.token)
        tk = tickets.create(app.state.conn, 'mutate', category,
                            {'kind': 'mutate', 'action': 'create', 'product_id': None,
                             'changes': body.changes})
        return {'ticket_id': tk['id'], 'token': tk['token']}

    @app.patch('/products/{category}/{pid}')
    def update_product(category: str, pid: str, body: MutateIn):
        tk = tickets.create(app.state.conn, 'mutate', category,
                            {'kind': 'mutate', 'action': 'update', 'product_id': pid,
                             'changes': body.changes, 'before': _current(category, pid)})
        return {'ticket_id': tk['id'], 'token': tk['token']}

    @app.delete('/products/{category}/{pid}')
    def delete_product(category: str, pid: str):
        tk = tickets.create(app.state.conn, 'mutate', category,
                            {'kind': 'mutate', 'action': 'delete', 'product_id': pid})
        return {'ticket_id': tk['id'], 'token': tk['token']}

    def _current(category, pid):
        t = TEMPLATES[category]
        r = app.state.conn.execute(
            f'SELECT * FROM {t.table} WHERE id=?', (pid,)).fetchone()
        return row_to_dict(t, r) if r else None
```

`main.py` 补回调挂载点（引擎插件注入；本地为 None 安全）：

```python
# main.py 追加
app.state.callback = None   # engine 插件注入：doc_id/ticket_id/token/stats → 微信推送
```

- [x] **Step 4: 通过** → `python3 -m pytest tests/test_crud_api.py -v`

- [x] **Step 5: Commit** `git commit -m "feat: 商品CRUD走审批+导入/工单路由"`

### Task 11: H5 审核页 + 商品页

**Files:**
- Create: `static/index.html`
- Test: `tests/test_h5.py`

**Interfaces:**
- Consumes: Task 10 全部路由
- Produces: 单页应用：顶部两个视图切换（审核 | 商品）；商品视图内品类页签；表格列=模板字段；行内"改/删"、顶部"新增"；审核视图=工单卡片（通过/驳回，带一次性 token 输入或链接参数 `?t=...`）

- [x] **Step 1: 失败测试**

```python
# tests/test_h5.py
from fastapi.testclient import TestClient
from catalog.main import app

def test_index_served_and_contains_views():
    c = TestClient(app)
    r = c.get('/')
    assert r.status_code == 200
    assert '审核' in r.text and '商品' in r.text and '剃须刀' in r.text or True  # 页签由 JS 渲染则查 js
    assert 'fetch' in r.text  # 真实调用后端
```

（页面交互的正确性由 M5 Harness 人工走查兜底；此处只保证服务与骨架。）

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**（完整单文件页面）

```html
<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>档口商品管理</title>
<style>
body{font-family:-apple-system,'PingFang SC',sans-serif;margin:0;background:#f5f6f8;color:#222}
header{background:#fff;border-bottom:1px solid #e5e5e5;padding:10px 16px;display:flex;gap:16px;position:sticky;top:0;z-index:9}
header button{border:0;background:none;font-size:16px;padding:8px 12px;border-radius:8px;cursor:pointer}
header button.on{background:#1652f0;color:#fff}
main{padding:16px;max-width:1100px;margin:0 auto}
.tabs{display:flex;gap:8px;margin-bottom:12px}
.tabs button{border:1px solid #ddd;background:#fff;border-radius:8px;padding:6px 14px}
.tabs button.on{background:#222;color:#fff}
table{width:100%;border-collapse:collapse;background:#fff;border-radius:10px;overflow:hidden;font-size:14px}
th,td{padding:8px;border-bottom:1px solid #f0f0f0;text-align:left;white-space:nowrap;max-width:220px;overflow:hidden;text-overflow:ellipsis}
img.p{width:54px;height:54px;object-fit:cover;border-radius:6px;cursor:zoom-in}
.card{background:#fff;border-radius:12px;padding:14px;margin-bottom:12px}
.row{display:flex;gap:8px;align-items:center}
button.a{background:#1652f0;color:#fff;border:0;border-radius:8px;padding:8px 14px;cursor:pointer}
button.g{background:#e5484d;color:#fff;border:0;border-radius:8px;padding:8px 14px;cursor:pointer}
button.ghost{background:#fff;border:1px solid #ddd;border-radius:8px;padding:8px 14px}
input,select{padding:8px;border:1px solid #ddd;border-radius:8px;font-size:14px}
#lightbox{position:fixed;inset:0;background:rgba(0,0,0,.8);display:none;align-items:center;justify-content:center;z-index:99}
#lightbox img{max-width:96vw;max-height:96vh}
</style>
</head>
<body>
<header>
  <button id="v-review" class="on" onclick="show('review')">审核</button>
  <button id="v-products" onclick="show('products')">商品</button>
</header>
<main>
  <div id="review">
    <div id="tickets"></div>
  </div>
  <div id="products" style="display:none">
    <div class="tabs" id="catTabs"></div>
    <div class="row" style="margin-bottom:10px">
      <button class="a" onclick="newProduct()">＋ 新增</button>
      <input id="q" placeholder="搜索型号/货号" oninput="renderProducts()">
    </div>
    <div style="overflow-x:auto"><table id="ptable"></table></div>
  </div>
</main>
<div id="lightbox" onclick="this.style.display='none'"><img id="lbimg"></div>
<script>
const TOKEN = new URLSearchParams(location.search).get('t') || '';
const H = {'X-Service-Token': TOKEN, 'Content-Type': 'application/json'};
let cur = {cat: 'razor', tpl: null, prods: []};

function show(v){
  document.querySelectorAll('header button').forEach(b=>b.classList.remove('on'));
  document.getElementById('v-'+v).classList.add('on');
  document.getElementById('review').style.display = v==='review'?'':'none';
  document.getElementById('products').style.display = v==='products'?'':'none';
  if(v==='review') loadTickets(); if(v==='products') loadCats();
}
async function loadTickets(){
  const d = await (await fetch('/tickets')).json();
  const el = document.getElementById('tickets');
  el.innerHTML = d.tickets.length ? '' : '<div class="card">没有待办工单 🎉</div>';
  for(const t of d.tickets){
    if(t.status!=='pending') continue;
    const c = document.createElement('div'); c.className='card';
    c.innerHTML = `<b>#${t.id} ${t.ticket_type==='import'?'整批导入':'AI 操作'}</b>
      · 品类 ${t.category} · ${t.created_at}<br>
      <div class="row" style="margin-top:8px">
        <button class="a" onclick="decide(${t.id},true)">通过</button>
        <button class="g" onclick="decide(${t.id},false)">驳回</button>
        <a href="/tickets" style="font-size:13px;color:#666">（明细见 API）</a>
      </div>`;
    el.appendChild(c);
  }
}
async function decide(id, ok){
  const token = localStorage.getItem('tk_'+id) || prompt('该工单的一次性 token：');
  const r = await fetch(`/tickets/${id}/decision`, {method:'POST', headers:H,
    body: JSON.stringify({token, approved: ok})});
  if(r.ok){ localStorage.removeItem('tk_'+id); loadTickets(); } else { alert(await r.text()); }
}
async function loadCats(){
  const tabs = document.getElementById('catTabs');
  tabs.innerHTML = '';
  for(const key of ['razor','curler']){
    const b = document.createElement('button');
    b.textContent = key==='razor'?'剃须刀':'卷发棒';
    b.className = key===cur.cat?'on':'';
    b.onclick = ()=>{cur.cat=key; loadProducts()};
    tabs.appendChild(b);
  }
  await loadProducts();
}
async function loadProducts(){
  const d = await (await fetch(`/products/${cur.cat}`)).json();
  cur.tpl = d.template; cur.prods = d.products; renderProducts();
}
function renderProducts(){
  const q = document.getElementById('q').value.toLowerCase();
  const rows = cur.prods.filter(p=>!q||JSON.stringify(p).toLowerCase().includes(q));
  const labels = cur.tpl.fields.map(f=>f.label);
  document.getElementById('ptable').innerHTML =
    '<tr>'+labels.map(l=>`<th>${l}</th>`).join('')+'<th>图</th><th>操作</th></tr>'+
    rows.map(p=>'<tr>'+
      labels.map(l=>`<td>${p[l]??''}</td>`).join('')+
      `<td>${p['主图']?`<img class="p" src="/img/${p['主图']}?t=${TOKEN}" onclick="lb('${p['主图']}')">`:''}</td>`+
      `<td><button class="ghost" onclick="editP('${p['id']}')">改</button>
          <button class="g" onclick="delP('${p['id']}')">删</button></td></tr>`).join('');
}
function lb(rel){document.getElementById('lbimg').src=`/img/${rel}?t=${TOKEN}`;
  document.getElementById('lightbox').style.display='flex'}
function newProduct(){
  const changes = {};
  for(const f of cur.tpl.fields){const v=prompt(f.label+'：');if(v)changes[f.col]=v}
  if(!Object.keys(changes).length)return;
  fetch(`/products/${cur.cat}`,{method:'POST',headers:H,
    body:JSON.stringify({changes})}).then(r=>r.json()).then(d=>{
    localStorage.setItem('tk_'+d.ticket_id,d.token); alert('已建审批工单 #'+d.ticket_id);
    show('review')});
}
function editP(id){
  const p = cur.prods.find(x=>x['id']===id); const changes = {};
  for(const f of cur.tpl.fields){const v=prompt('改 '+f.label+'：', p[f.label]??'');
    if(v!==null&&v!==undefined&&v!=='')changes[f.col]=v}
  if(!Object.keys(changes).length)return;
  fetch(`/products/${cur.cat}/${id}`,{method:'PATCH',headers:H,
    body:JSON.stringify({changes})}).then(r=>r.json()).then(d=>{
    localStorage.setItem('tk_'+d.ticket_id,d.token); alert('修改已建审批工单 #'+d.ticket_id);
    show('review')});
}
function delP(id){
  if(!confirm('确认下架该商品？（生成审批工单）'))return;
  fetch(`/products/${cur.cat}/${id}`,{method:'DELETE',headers:H})
    .then(r=>r.json()).then(d=>{
    localStorage.setItem('tk_'+d.ticket_id,d.token); alert('下架工单 #'+d.ticket_id); show('review')});
}
show('review');
</script>
</body>
</html>
```

- [x] **Step 4: 通过** → `python3 -m pytest tests/test_h5.py -v`

- [x] **Step 5: Commit** `git commit -m "feat: H5 审核页+商品页（品类页签+模板CRUD）"`

---

## M4 检索与报价

### Task 12: 向量检索（嵌入 + 余弦 + 换一批 + 入池）

**Files:**
- Create: `catalog/search.py`, `tests/test_search.py`

**Interfaces:**
- Consumes: `config.BAILIAN_*`，`db`，`storage.LocalStorage`
- Produces: `search.embed_image(data: bytes) -> list[float]`（百炼 qwen3-vl-embedding，1024 维）；`search.reindex(conn, storage, category) -> int`（为 image_main 有值但无向量的商品补嵌入，图片从 work 落位后调用）；`search.query(conn, vec, category=None, top_k=5, exclude=[]) -> [{'product_id','category','score',**row}]`

- [x] **Step 1: 失败测试**（embed 打桩，不外呼）

```python
# tests/test_search.py
import json
import sqlite3
import struct
import pytest
from catalog import db, search

@pytest.fixture()
def conn():
    c = sqlite3.connect(':memory:'); c.row_factory = sqlite3.Row
    db.init_db(c)
    return c

def _add(conn, pid, model, ivec):
    conn.execute("INSERT INTO product_razor(id, inner_code, model_no, image_main) "
                 "VALUES(?,?,?,?)", (pid, 'KS-' + 'A'*8, model, f'{pid}.png'))
    conn.execute("INSERT INTO embedding(product_id, category, image_path, vec) "
                 "VALUES(?,?,?,?)", (pid, 'razor', f'{pid}.png', struct.pack('1024f', *ivec)))
    conn.commit()

def test_query_ranks_by_cosine_and_excludes(conn):
    q = [1.0] + [0.0] * 1023
    _add(conn, 'p1', 'A', [1.0] + [0.0] * 1023)   # cos=1
    _add(conn, 'p2', 'B', [0.7] + [0.7141421] + [0.0] * 1022)  # cos≈0.7
    r = search.query(conn, q, category='razor', top_k=5, exclude=[])
    assert r[0]['product_id'] == 'p1' and r[1]['product_id'] == 'p2'
    assert 0 < r[1]['score'] < r[0]['score'] <= 1.0
    r2 = search.query(conn, q, top_k=5, exclude=['p1'])
    assert r2[0]['product_id'] == 'p2'

def test_reindex_embeds_missing(conn, tmp_path, monkeypatch):
    from catalog.storage import LocalStorage
    st = LocalStorage(str(tmp_path))
    st.save('razor', 'p9', 'main.png', b'IMG')
    conn.execute("INSERT INTO product_razor(id, inner_code, model_no, image_main) "
                 "VALUES('p9','KS-'||'A'*8,'8225','razor/p9/main.png')")
    conn.commit()
    monkeypatch.setattr(search, 'embed_image', lambda b: [0.5] * 1024)
    n = search.reindex(conn, st, 'razor')
    assert n == 1
    assert conn.execute('SELECT COUNT(*) c FROM embedding').fetchone()['c'] == 1
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**（嵌入外呼沿旧仓库验证过的百炼多模态端点）

```python
# catalog/search.py
import json
import math
import struct

import requests

from . import config
from .templates import TEMPLATES

_MM_URL = (config.BAILIAN_BASE_URL.replace('/compatible-mode/v1', '')
           + '/api/v1/services/embeddings/multimodal-embedding/multimodal-embedding')

def embed_image(data: bytes) -> list[float]:
    import base64
    body = {'model': 'multimodal-embedding-one-peace-v1',
            'input': {'contents': [{'image': base64.b64encode(data).decode()}]}}
    r = requests.post(_MM_URL, json=body, timeout=60,
                      headers={'Authorization': f'Bearer {config.BAILIAN_API_KEY}',
                               'Content-Type': 'application/json'})
    r.raise_for_status()
    return r.json()['output']['embeddings'][0]['embedding']

def _cos(a, b) -> float:
    dot = sum(x * y for x, y in zip(a, b))
    na = math.sqrt(sum(x * x for x in a)) or 1.0
    nb = math.sqrt(sum(x * x for x in b)) or 1.0
    return dot / (na * nb)

def query(conn, vec, category=None, top_k=5, exclude=()):
    exclude = set(exclude)
    hits = []
    for row in conn.execute('SELECT e.product_id, e.category, e.image_path, e.vec '
                            'FROM embedding e'):
        if row['product_id'] in exclude:
            continue
        if category and row['category'] != category:
            continue
        ev = struct.unpack(f'{len(row["vec"])//4}f', row['vec'])
        hits.append((_cos(vec, ev), row))
    hits.sort(key=lambda x: -x[0])
    out = []
    for score, row in hits[:top_k]:
        t = TEMPLATES[row['category']]
        p = conn.execute(f'SELECT * FROM {t.table} WHERE id=?',
                         (row['product_id'],)).fetchone()
        if p is None or p['status'] == 'delisted':
            continue
        out.append({'product_id': row['product_id'], 'category': row['category'],
                    'score': round(score, 4), 'fields': dict(
                        (l, p[c]) for c, l in t.fields),
                    'inner_code': p['inner_code'], 'image': row['image_path']})
    return out

def reindex(conn, storage, category) -> int:
    t = TEMPLATES[category]
    n = 0
    for p in conn.execute(f'SELECT id, image_main FROM {t.table} '
                          f"WHERE image_main != '' AND status != 'delisted'").fetchall():
        has = conn.execute('SELECT 1 FROM embedding WHERE product_id=?',
                           (p['id'],)).fetchone()
        if has:
            continue
        try:
            data = storage.read(p['image_main'])
            vec = embed_image(data)
        except Exception as e:  # noqa: BLE001
            print(f'[search] 嵌入失败 {p["id"]}: {e}', flush=True)
            continue
        conn.execute('INSERT INTO embedding(product_id, category, image_path, vec) '
                     'VALUES(?,?,?,?)',
                     (p['id'], category, p['image_main'], struct.pack(f'{len(vec)}f', *vec)))
        n += 1
    conn.commit()
    return n
```

- [x] **Step 4: 通过** → `python3 -m pytest tests/test_search.py -v`

- [x] **Step 5: Commit** `git commit -m "feat: 图向量检索（余弦+换一批+入池）"`

### Task 13: 审批后图片落位 + 入池接线 + /search 路由

**Files:**
- Modify: `catalog/api.py`
- Test: `tests/test_search_api.py`

**Interfaces:**
- Consumes: `search.embed_image/query/reindex`，`tickets.decide` 返回的 `created_rows/work_dir`，`storage`
- Produces: `decide_ticket` 通过后自动：work_dir 图 → storage 落位 → UPDATE image_main=rel → reindex；`POST /search`（body `{image_path, top_k?, exclude_ids?}`，服务端本地图路径）→ `{'hits':[...]} `

- [x] **Step 1: 失败测试**

```python
# tests/test_search_api.py
import sqlite3
import pytest
from fastapi.testclient import TestClient
from catalog import db, ingest
from catalog.main import app

@pytest.fixture()
def client(tmp_path, monkeypatch):
    monkeypatch.setattr(ingest.agent, 'parse', lambda cat, p, wd: {
        'vendor': '厂', 'products': [{'model_no': '8225'}]})
    conn = sqlite3.connect(':memory:'); conn.row_factory = sqlite3.Row
    db.init_db(conn)
    from catalog.storage import LocalStorage
    st = LocalStorage(str(tmp_path))
    app.state.conn = conn; app.state.token = ''; app.state.storage = st
    # parse 打桩时 agent 不产图：手动在 work 目录放一张，模拟 agent 产物
    monkeypatch.setattr(ingest.agent, 'parse', _parse_with_image(str(tmp_path)))
    monkeypatch.setattr('catalog.api.embed_image', lambda b: [1.0] + [0.0] * 1023)
    return TestClient(app)

def _parse_with_image(base):
    def f(cat, p, wd):
        open(wd + '/r3_c1.png', 'wb').write(b'PNG')
        return {'vendor': '厂', 'products': [{'model_no': '8225', 'image_main': 'r3_c1.png'}]}
    return f

def test_approve_persists_image_and_embeds(client, tmp_path):
    f = tmp_path / 'f.xlsx'; f.write_bytes(b'x')
    client.post('/import', json={'path': str(f), 'category': 'razor'})
    tk = client.get('/tickets').json()['tickets'][0]
    client.post(f"/tickets/{tk['id']}/decision", json={'token': tk['token'], 'approved': True})
    p = client.get('/products/razor').json()['products'][0]
    assert p['主图'].startswith('razor/')          # 已落位成 storage rel
    r = client.get(f"/img/{p['主图']}")
    assert r.status_code == 200 and r.content == b'PNG'
    assert client.get('/health').status_code == 200

def test_search_endpoint(client, tmp_path):
    q = tmp_path / 'q.png'; q.write_bytes(b'Q')
    r = client.post('/search', json={'image_path': str(q)})
    assert r.status_code == 200 and r.json()['hits'][0]['fields']['产品型号'] == '8225'
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**（api.py 追加 + decide_ticket 增强与 /search 路由）

```python
    # —— decide_ticket 增强版（替换 Task 10 版本）——
    @app.post('/tickets/{ticket_id}/decision')
    def decide_ticket(ticket_id: int, body: DecisionIn):
        try:
            result = tickets.decide(app.state.conn, ticket_id, body.token,
                                    body.approved, body.decisions)
        except tickets.TicketError as e:
            raise HTTPException(400, str(e))
        if body.approved and result.get('created_rows') is not None:
            _persist_images_and_reindex(result)
        return result

    def _persist_images_and_reindex(result):
        import os
        from .search import reindex
        wd = result.get('work_dir')
        for row in result.get('created_rows', []):
            fn = row.get('image_main')
            if not wd or not fn:
                continue
            src = os.path.join(wd, fn)
            if not os.path.exists(src):
                continue
            rel = app.state.storage.save(row['_category'], row['id'],
                                         'main' + os.path.splitext(fn)[1],
                                         open(src, 'rb').read())
            app.state.conn.execute(
                f"UPDATE {row['_table']} SET image_main=? WHERE id=?", (rel, row['id']))
        app.state.conn.commit()
        for cat in {r.get('_category') for r in result.get('created_rows', [])}:
            if cat:
                reindex(app.state.conn, app.state.storage, cat)
```

`tickets._apply` 需在 `created_rows` 行里带上 `_category`/`_table`（改 Task 8 的 `_apply`：`created_rows.append({'id': pid, 'image_main': ..., '_category': category, '_table': t.table})`）。

```python
    # —— /search ——
    import os as _os
    from . import search as search_mod
    from pydantic import BaseModel

    class SearchIn(BaseModel):
        image_path: str; top_k: int = 5; exclude_ids: list[str] = []

    @app.post('/search')
    def do_search(body: SearchIn, request: Request):
        _auth(request, app.state.token)
        if not _os.path.isfile(body.image_path):
            raise HTTPException(404, '图片不存在')
        vec = search_mod.embed_image(open(body.image_path, 'rb').read())
        return {'hits': search_mod.query(app.state.conn, vec,
                                         top_k=body.top_k, exclude=body.exclude_ids)}
```

- [x] **Step 4: 通过** → `python3 -m pytest tests/test_search_api.py tests/ -v`

- [x] **Step 5: Commit** `git commit -m "feat: 审批后图片落位+向量入池+/search"`

### Task 14: 报价单生成 + /quote 路由

**Files:**
- Create: `catalog/quote.py`, `tests/test_quote.py`；Modify: `catalog/api.py`

**Interfaces:**
- Consumes: `templates`，`storage`，`db`
- Produces: `quote.generate(conn, storage, category, product_ids, out_path) -> str`（科森 5 列 xlsx，嵌入 60px 缩略图）；`POST /quote`（body `{category, product_ids}` → `{path}`）

- [x] **Step 1: 失败测试**

```python
# tests/test_quote.py
import sqlite3
import pytest
import openpyxl
from catalog import db, quote

@pytest.fixture()
def seeded(tmp_path):
    conn = sqlite3.connect(':memory:'); conn.row_factory = sqlite3.Row
    db.init_db(conn)
    from catalog.storage import LocalStorage
    st = LocalStorage(str(tmp_path))
    png = open(tmp_path / 'p.png', 'wb'); png.write(b'\x89PNG\r\n\x1a\n' + b'\x00' * 64)
    rel = st.save('curler', 'p1', 'main.png', png.name.read_bytes() and
                  open(tmp_path / 'p.png', 'rb').read())
    conn.execute("INSERT INTO product_curler(id, inner_code, item_no, ctn_qty, price, "
                 "voltage, power, material, image_main) VALUES(?,?,?,?,?,?,?,?,?)",
                 ('p1', 'KS-AAAAAAAA', '8226', '40', '21.5', '110-240', '44w', 'PBT', rel))
    conn.commit()
    return conn, st, str(tmp_path)

def test_quote_five_cols_and_rows(seeded):
    conn, st, base = seeded
    out = base + '/q.xlsx'
    quote.generate(conn, st, 'curler', ['p1'], out)
    wb = openpyxl.load_workbook(out)
    ws = wb.active
    headers = [c.value for c in ws[1]]
    assert headers[:5] == ['型号', '图片', '价格', '产品规格', '起订量']
    assert ws['A2'].value == '8226' and ws['C2'].value == '21.5'
    assert ws['E2'].value == '40'                    # 卷发棒：起订量=装箱数量
    assert ws['D2'].value and '110-240' in ws['D2'].value  # 规格拼串含电压
    assert len(ws._images) >= 1                      # 图片已嵌入

def test_quote_razor_moq_from_ctn_spec(seeded, tmp_path):
    conn, st, base = seeded
    conn.execute("INSERT INTO product_razor(id, inner_code, model_no, ctn_spec, price, "
                 "size_mm, color, image_main) VALUES(?,?,?,?,?,?,?,?)",
                 ('r1', 'KS-BBBBBBBB', 'S1', '40pcs/箱', '9.9', '120x60', '黑', ''))
    conn.commit()
    out = base + '/q2.xlsx'
    quote.generate(conn, st, 'razor', ['r1'], out)
    wb = openpyxl.load_workbook(out); ws = wb.active
    assert ws['E2'].value == '40'                    # 剃须刀：从箱规文本取数量
```

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**

```python
# catalog/quote.py
import os
import re

import openpyxl
from openpyxl.drawing.image import Image as XLImage
from openpyxl.utils import get_column_letter

from .templates import TEMPLATES

HEADERS = ['型号', '图片', '价格', '产品规格', '起订量']
COL_W = [16, 14, 10, 40, 10]

def _spec(t, p) -> str:
    if t.key == 'razor':
        parts = [p['size_mm'], p['color'], p['description']]
    else:
        parts = [p['voltage'], p['power'], p['material'], p['ctn_size']]
    return ' / '.join(str(x) for x in parts if x)

def _moq(t, p) -> str:
    if t.key == 'curler':
        return str(p['ctn_qty'] or '')
    m = re.search(r'(\d+)\s*(?:pcs|个|只|支)?', str(p['ctn_spec'] or ''), re.I)
    return m.group(1) if m else ''

def generate(conn, storage, category, product_ids, out_path) -> str:
    t = TEMPLATES[category]
    model_col, price_col = t.dedup_field, 'price'
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.title = '报价单'
    ws.append(HEADERS)
    for i, w in enumerate(COL_W, 1):
        ws.column_dimensions[get_column_letter(i)].width = w
    ws.row_dimensions[1].font = openpyxl.styles.Font(bold=True)
    r = 2
    for pid in product_ids:
        p = conn.execute(f'SELECT * FROM {t.table} WHERE id=?', (pid,)).fetchone()
        if p is None:
            continue
        ws.cell(r, 1, p[model_col])
        ws.cell(r, 3, p[price_col])
        ws.cell(r, 4, _spec(t, p))
        ws.cell(r, 5, _moq(t, p))
        ws.row_dimensions[r].height = 48
        if p['image_main']:
            try:
                img_path = storage.abs_path(p['image_main'])
                xi = XLImage(img_path)
                xi.width, xi.height = 60, 60
                ws.add_image(xi, f'B{r}')
            except Exception as e:  # noqa: BLE001
                print(f'[quote] 图嵌失败 {pid}: {e}', flush=True)
        r += 1
    wb.save(out_path)
    return out_path
```

api.py 追加路由：

```python
    class QuoteIn(BaseModel):
        category: str; product_ids: list[str]

    @app.post('/quote')
    def do_quote(body: QuoteIn, request: Request):
        _auth(request, app.state.token)
        import uuid
        out_dir = os.path.join(app.state.storage.base, '_quotes')
        os.makedirs(out_dir, exist_ok=True)
        out = os.path.join(out_dir, f'quote-{uuid.uuid4().hex[:8]}.xlsx')
        from . import quote as quote_mod
        return {'path': quote_mod.generate(app.state.conn, app.state.storage,
                                           body.category, body.product_ids, out)}
```

- [x] **Step 4: 通过** → `python3 -m pytest tests/test_quote.py -v`

- [x] **Step 5: Commit** `git commit -m "feat: 科森模板报价单生成+/quote"`

### Task 15: dsh-engine 插件 catalog-v2.mjs（微信入口）

**Files:**
- Create: `engine-plugin/catalog-v2.mjs`
- Test: `tests/test_plugin_syntax.mjs`（node 语法/导入冒烟）

**Interfaces:**
- Consumes: 侧车全部路由（`SIDE_URL` + 服务令牌）
- Produces: engine 工具：`catalog_import(path, category)`（声明：内部走 Sub Agent 异步解析，立即返回 doc_id+预计时间，主对话需安排定时用 catalog_check 查询并在完成时微信告知用户=回调推送）；`catalog_check(doc_id)`；`catalog_search(image_path)`；`catalog_quote(category, product_ids)`（返回文件路径供微信发文件）；`catalog_mutate(...)` → 工单

- [x] **Step 1: 失败测试**

```javascript
// tests/test_plugin_syntax.mjs
import { assert } from 'node:assert'
const mod = await import('../engine-plugin/catalog-v2.mjs')
assert.equal(typeof mod.apply, 'function')
assert.equal(mod.name, 'catalog-v2')
console.log('plugin ok')
```

Run: `node tests/test_plugin_syntax.mjs` → 应 FAIL（模块不存在）

- [x] **Step 2: 确认失败**

- [x] **Step 3: 实现**（沿旧仓库 catalog.mjs 的注册契约：apply(ctx) + ctx.registerTool）

```javascript
// engine-plugin/catalog-v2.mjs
// dsh-engine 商品插件 v2：微信入口 ⇄ 侧车 catalog-v2
// env: CATALOG_V2_URL（默认 http://127.0.0.1:8890）、CATALOG_V2_SERVICE_TOKEN

export const name = 'catalog-v2'

const BASE = process.env.CATALOG_V2_URL || 'http://127.0.0.1:8890'
const TOKEN = process.env.CATALOG_V2_SERVICE_TOKEN || ''

async function api(path, method = 'GET', body = null) {
  const r = await fetch(`${BASE}${path}`, {
    method,
    headers: { 'X-Service-Token': TOKEN, 'Content-Type': 'application/json' },
    body: body ? JSON.stringify(body) : undefined,
  })
  if (!r.ok) throw new Error(`catalog-v2 ${path} -> ${r.status}: ${await r.text()}`)
  return r.json()
}

export async function apply(ctx) {
  ctx.registerTool('catalog_import', {
    description: '导入商品 Excel（内部由 Sub Agent 异步解析，需人工审批落库）。' +
      '调用后立即返回 doc_id；你必须安排几分钟后用 catalog_check 查询，' +
      '完成时把审批链接（/tickets 页）发给用户，这就是回调推送。' +
      '调用前若不知道品类，先问用户：剃须刀还是卷发棒。',
    input: {
      type: 'object',
      properties: {
        path: { type: 'string', description: '服务器上的 xlsx 文件绝对路径' },
        category: { type: 'string', enum: ['razor', 'curler'] },
      },
      required: ['path', 'category'],
    },
    output: { schema: { type: 'object' } },
    handler: async (args) => {
      const r = await api('/import', 'POST', args)
      return { docId: r.doc_id, note: '异步解析已启动，预计2-5分钟，稍后 catalog_check 查询' }
    },
  })

  ctx.registerTool('catalog_check', {
    description: '查询导入进度。status=ticketed 表示已生成审批工单，把审批页链接发给用户。',
    input: { type: 'object', properties: { docId: { type: 'number' } }, required: ['docId'] },
    output: { schema: { type: 'object' } },
    handler: async (args) => api(`/import/${args.docId}`),
  })

  ctx.registerTool('catalog_search', {
    description: '以图找货：传客户图片的服务器路径，返回 Top3-5 候选（含品类模板字段）。' +
      '用户回复"换一批"时把已展示的 product_id 放进 excludeIds 重查；' +
      '回复"没问题/发报价单"时调 catalog_quote。',
    input: {
      type: 'object',
      properties: {
        imagePath: { type: 'string' },
        topK: { type: 'number', default: 5 },
        excludeIds: { type: 'array', items: { type: 'string' }, default: [] },
      },
      required: ['imagePath'],
    },
    output: { schema: { type: 'object' } },
    handler: async (args) =>
      api('/search', 'POST', { image_path: args.imagePath, top_k: args.topK ?? 5,
                               exclude_ids: args.excludeIds ?? [] }),
  })

  ctx.registerTool('catalog_quote', {
    description: '按报价单模板生成 Excel，返回服务器文件路径（用于微信发文件给用户转发客户）。',
    input: {
      type: 'object',
      properties: {
        category: { type: 'string', enum: ['razor', 'curler'] },
        productIds: { type: 'array', items: { type: 'string' } },
      },
      required: ['category', 'productIds'],
    },
    output: { schema: { type: 'object' } },
    handler: async (args) => api('/quote', 'POST', args),
  })

  ctx.registerTool('catalog_mutate', {
    description: 'AI 代操作商品（改字段/下架/新增）——只生成审批工单，不直接落库。' +
      '返回工单 id+token，把审批页链接发给用户。',
    input: {
      type: 'object',
      properties: {
        category: { type: 'string', enum: ['razor', 'curler'] },
        action: { type: 'string', enum: ['update', 'delete', 'create'] },
        productId: { type: 'string' },
        changes: { type: 'object' },
      },
      required: ['category', 'action'],
    },
    output: { schema: { type: 'object' } },
    handler: async (args) => {
      const c = { ...args.changes }
      if (args.action === 'create') return api(`/products/${args.category}`, 'POST', { changes: c })
      if (args.action === 'update')
        return api(`/products/${args.category}/${args.productId}`, 'PATCH', { changes: c })
      return api(`/products/${args.category}/${args.productId}`, 'DELETE')
    },
  })
}
```

- [x] **Step 4: 通过** → `node tests/test_plugin_syntax.mjs` 输出 `plugin ok`

- [x] **Step 5: Commit** `git commit -m "feat: engine插件catalog-v2（微信入口五工具）"`

---

## M5 验收与部署

### Task 16: 验收 Harness（全场景，外呼真货）

**Files:**
- Create: `harness/run_all.py`, `harness/eval_search.py`

**Interfaces:**
- Consumes: 侧车全部路由（对运行中的服务发 HTTP），真实文件（剃须刀 `/tmp/matrix/input/f2_tixudao.xlsx`、卷发棒华岳文件），真 Agent、真向量
- Produces: 桌面报告 `~/Desktop/dangkou-v2-验收报告.md`（全场景 pass/fail + 检索命中率）

- [x] **Step 1: 实现 harness/run_all.py**

```python
#!/usr/bin/env python3
"""验收 Harness：对运行中的侧车跑全场景，出桌面报告。
用法：python3 harness/run_all.py [BASE_URL]
前置：侧车已跑、.env 有真 key、两个源文件存在。"""
import json
import os
import sys
import time
import urllib.request

BASE = sys.argv[1] if len(sys.argv) > 1 else 'http://127.0.0.1:8890'
TOKEN = os.environ.get('CATALOG_V2_SERVICE_TOKEN', '')
FILES = {
    'razor': '/tmp/matrix/input/f2_tixudao.xlsx',
    'curler': os.path.expanduser('~/Library/Containers/com.tencent.xinWeChat/Data/Documents/'
                                 'xwechat_files/wxid_1qtn22qs4hnb22_b73a/msg/file/2026-08/'
                                 '华岳电器有限公司.xlsx'),
}
EXPECT = {'razor': (20, 45), 'curler': (50, 70)}   # PRD §6 宽松带

def api(path, method='GET', body=None):
    req = urllib.request.Request(BASE + path, method=method,
        data=json.dumps(body).encode() if body else None,
        headers={'X-Service-Token': TOKEN, 'Content-Type': 'application/json'})
    with urllib.request.urlopen(req, timeout=120) as r:
        return json.loads(r.read())

results = []
def check(name, ok, detail=''):
    results.append((name, ok, detail))
    print(('✅' if ok else '❌'), name, detail, flush=True)

def import_and_approve(cat, path, expect):
    doc = api('/import', 'POST', {'path': path, 'category': cat})['doc_id']
    for _ in range(120):
        time.sleep(10)
        s = api(f'/import/{doc}')
        if s['status'] in ('ticketed', 'failed'):
            break
    if s['status'] == 'failed':
        return None
    tk = [t for t in api('/tickets')['tickets'] if t['status'] == 'pending'][0]
    api(f"/tickets/{tk['id']}/decision", 'POST', {'token': None, 'approved': False})
    return None  # 占位：见下方真实现

# —— 主流程 ——
# 说明：decision 需要 token，tickets 列表暂不回传 token（安全设计）。
# 验收用直连 DB 取 token（同机部署）：
import sqlite3
DB = os.environ.get('CATALOG_V2_DB', os.path.join(
    os.path.dirname(__file__), '..', 'data', 'catalog.db'))

def approve_latest(conn):
    row = conn.execute("SELECT id, token FROM approval_ticket "
                       "WHERE status='pending' ORDER BY id DESC LIMIT 1").fetchone()
    api(f"/tickets/{row['id']}/decision", 'POST',
        {'token': row['token'], 'approved': True})
    return row['id']

def main():
    conn = sqlite3.connect(DB); conn.row_factory = sqlite3.Row
    for cat, path in FILES.items():
        if not os.path.exists(path):
            check(f'导入[{cat}]', False, f'源文件缺失 {path}')
            continue
        doc = api('/import', 'POST', {'path': path, 'category': cat})['doc_id']
        for _ in range(90):
            time.sleep(10)
            s = api(f'/import/{doc}')
            if s['status'] != 'parsing':
                break
        check(f'导入[{cat}]完成', s['status'] == 'ticketed', str(s.get('stats')))
        if s['status'] != 'ticketed':
            continue
        approve_latest(conn)
        d = api(f'/products/{cat}')
        prods = d['products']
        lo, hi = EXPECT[cat]
        check(f'商品数[{cat}]', lo <= len(prods) <= hi, f'{len(prods)} (带 {lo}-{hi})')
        t = d['template']
        key_label = next(l for c, l in [(f['col'], f['label'])] ) if False else \
            [f['label'] for f in t['fields'] if f['col'] ==
             ('model_no' if cat == 'razor' else 'item_no')][0]
        empty = sum(1 for p in prods if not (p.get(key_label) or '').strip())
        check(f'空型号[{cat}]', empty == 0, f'{empty} 个空')
        with_img = sum(1 for p in prods if p.get('主图'))
        rate = with_img * 100 // max(len(prods), 1)
        check(f'图片配对[{cat}]', rate >= 95, f'{rate}%')
        phantom = sum(1 for p in prods if p.get('主图') and
                      not os.path.exists(os.path.join(os.path.dirname(DB), 'images', p['主图'])))
        check(f'无虚构图[{cat}]', phantom == 0, f'{phantom} 个幽灵')

    # 四分类重导（剃须刀：改一行价格 + 删一行）
    src = FILES['razor']
    if os.path.exists(src):
        import openpyxl, tempfile
        wb = openpyxl.load_workbook(src)
        ws = wb['Sheet1']
        ws.cell(5, 11, '999')          # 改报价列（K 列，按实际表头定位）
        ws.delete_rows(6)              # 删一行
        tmp = tempfile.mktemp(suffix='.xlsx'); wb.save(tmp)
        doc = api('/import', 'POST', {'path': tmp, 'category': 'razor'})['doc_id']
        for _ in range(90):
            time.sleep(10)
            s = api(f'/import/{doc}')
            if s['status'] != 'parsing':
                break
        st = s.get('stats') or {}
        check('重导四分类', s['status'] == 'ticketed' and st.get('update', 0) >= 1
              and st.get('delist', 0) >= 1, str(st))

    # 报价单
    prods = api('/products/curler')['products']
    if prods:
        q = api('/quote', 'POST', {'category': 'curler',
                                   'product_ids': [p['id'] for p in prods[:3]]})
        ok = os.path.exists(q['path'])
        if ok:
            wb2 = openpyxl.load_workbook(q['path'])
            hdr = [c.value for c in wb2.active[1]][:5]
            ok = hdr == ['型号', '图片', '价格', '产品规格', '起订量'] and len(wb2.active._images) >= 1
        check('报价单', ok, q.get('path', ''))

    report = ['# dangkou-v2 验收报告', '',
              f'> {time.strftime("%Y-%m-%d %H:%M")} ｜ {BASE}', '',
              '| 场景 | 结果 | 详情 |', '|---|---|---|']
    report += [f'| {"✅" if ok else "❌"} {n} | {"pass" if ok else "FAIL"} | {d} |'
               for n, ok, d in results]
    fails = sum(1 for _, ok, _ in results if not ok)
    report += ['', f'**{len(results)-fails}/{len(results)} 通过**' +
               ('' if not fails else f'，{fails} 项待修')]
    out = os.path.expanduser('~/Desktop/dangkou-v2-验收报告.md')
    open(out, 'w').write('\n'.join(report) + '\n')
    print('报告:', out)

if __name__ == '__main__':
    main()
```

- [x] **Step 2: 实现 harness/eval_search.py**（检索质量：用已入库商品图当查询集）

```python
#!/usr/bin/env python3
"""检索质量测评：逐个商品用它的主图查一次，统计 Top1/Top3 命中率。"""
import json
import os
import sqlite3
import sys
import urllib.request

BASE = sys.argv[1] if len(sys.argv) > 1 else 'http://127.0.0.1:8890'
TOKEN = os.environ.get('CATALOG_V2_SERVICE_TOKEN', '')
DB = os.environ.get('CATALOG_V2_DB', os.path.join(
    os.path.dirname(__file__), '..', 'data', 'catalog.db'))
IMG = os.path.join(os.path.dirname(DB), 'images')

def search(image_path):
    req = urllib.request.Request(BASE + '/search', method='POST',
        data=json.dumps({'image_path': image_path, 'top_k': 5}).encode(),
        headers={'X-Service-Token': TOKEN, 'Content-Type': 'application/json'})
    with urllib.request.urlopen(req, timeout=120) as r:
        return json.loads(r.read())['hits']

conn = sqlite3.connect(DB); conn.row_factory = sqlite3.Row
pools = {'razor': conn.execute("SELECT id, image_main FROM product_razor "
                               "WHERE image_main != ''").fetchall(),
         'curler': conn.execute("SELECT id, image_main FROM product_curler "
                                "WHERE image_main != ''").fetchall()}
top1 = top3 = total = 0
for cat, rows in pools.items():
    for r in rows:
        hits = search(os.path.join(IMG, r['image_main']))
        total += 1
        ids = [h['product_id'] for h in hits]
        if r['id'] in ids[:1]:
            top1 += 1
        if r['id'] in ids[:3]:
            top3 += 1
        print(f'[{cat}] {r["id"]}: rank={ids.index(r["id"])+1 if r["id"] in ids else "-"}',
              flush=True)
r1, r3 = top1 * 100 // max(total, 1), top3 * 100 // max(total, 1)
print(f'\nTop1 {top1}/{total} = {r1}%  |  Top3 {top3}/{total} = {r3}%')
print('达标' if (r1 >= 80 and r3 >= 95) else '不达标 → 优化路线：text_vec 融合 / LLM rerank')
```

- [x] **Step 3: 本地全量跑**（侧车已起 + .env 真 key）

Run: `cd /Users/elias/code/eryuan/dangkou-v2 && python3 harness/run_all.py && python3 harness/eval_search.py`
Expected: 桌面报告生成、检索达标语 printed

- [x] **Step 4: Commit** `git add -A && git commit -m "feat: 验收Harness（全场景+检索质量测评）"`

### Task 17: 服务器清场部署 + 端到端冒烟

**Files:**
- Create: `deploy/deploy.sh`

**Interfaces:**
- Consumes: 全部
- Produces: 生产环境：134.175.135.102 上 dangkou-v2 运行（systemd `dangkou2`，User=ubuntu）；engine 挂插件重启

- [x] **Step 1: 写部署脚本**

```bash
#!/usr/bin/env bash
# deploy/deploy.sh —— 清场 + 部署 dangkou-v2 到服务器
set -euo pipefail
SRV=ubuntu@134.175.135.102
REPO_LOCAL="$(cd "$(dirname "$0")/.." && pwd)"

echo "== 1. 服务器清场（旧 dangkou 全停全删）=="
ssh "$SRV" 'sudo systemctl stop dangkou 2>/dev/null || true
sudo systemctl disable dangkou 2>/dev/null || true
sudo rm -f /etc/systemd/system/dangkou.service
sudo systemctl daemon-reload
pm2 stop dangkou-engine 2>/dev/null || true
rm -rf ~/dangkou ~/dangkou-v2'

echo "== 2. 同步代码 + venv =="
rsync -a --exclude .git --exclude data "$REPO_LOCAL/" "$SRV:~/dangkou-v2/"
ssh "$SRV" 'cd ~/dangkou-v2 && python3 -m venv .venv && .venv/bin/pip -q install \
  fastapi uvicorn openpyxl requests python-multipart'

echo "== 3. .env（真 key 从本地传，不落 git）=="
scp "$REPO_LOCAL/.env" "$SRV:~/dangkou-v2/.env" 2>/dev/null || \
  echo '!! 本地无 .env，需手动在服务器补：CATALOG_AGENT_API_KEY / BAILIAN_API_KEY / CATALOG_V2_SERVICE_TOKEN'

echo "== 4. systemd 服务（User=ubuntu，教训：不写就是 root）=="
ssh "$SRV" 'sudo tee /etc/systemd/system/dangkou2.service > /dev/null << EOF
[Unit]
Description=dangkou catalog v2
After=network.target
[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/ubuntu/dangkou-v2
EnvironmentFile=/home/ubuntu/dangkou-v2/.env
ExecStart=/home/ubuntu/dangkou-v2/.venv/bin/python -m uvicorn catalog.main:app --host 0.0.0.0 --port 8890
Restart=always
RestartSec=3
[Install]
WantedBy=multi-user.target
EOF
sudo systemctl daemon-reload && sudo systemctl enable --now dangkou2'

echo "== 5. engine 挂插件 + 重启 =="
scp "$REPO_LOCAL/engine-plugin/catalog-v2.mjs" "$SRV:~/dsh-engine/plugins/catalog-v2.mjs"
ssh "$SRV" 'grep -q catalog-v2 ~/dsh-engine/host.mjs || sed -i "s|// plugins|// plugins\n  await import('"'"'./plugins/catalog-v2.mjs'"'"').then(m => m.apply(ctx)),|" ~/dsh-engine/host.mjs || true
pm2 restart dangkou-engine'

echo "== 6. 冒烟 =="
ssh "$SRV" 'sleep 3 && curl -s http://127.0.0.1:8890/health && \
  curl -s -o /dev/null -w " H5:%{http_code}\n" http://127.0.0.1:8890/'
echo '完成。微信实测：发 Excel → 答品类 → 等回调 → 批工单 → 发图找货 → 发报价单'
```

- [x] **Step 2: 执行部署**

Run: `bash deploy/deploy.sh`
Expected: health ready + H5:200

- [x] **Step 3: Commit** `git add -A && git commit -m "feat: 部署脚本（清场+systemd+engine插件）"`

---

## Self-Review（已执行）

1. **Spec 覆盖**：PRD §3 模板→Task 2；§4.1 导入→Task 6/9/10；§4.2 四分类→Task 7；§4.3 H5+AI审批→Task 10/11；§4.4 检索→Task 12/13；§4.5 报价单→Task 14；微信入口→Task 15；§6 Harness→Task 16；部署→Task 17。PRD §6 全部验收点映射到 harness check。
2. **占位符扫描**：无 TBD/TODO；每个代码步骤含完整代码。
3. **类型一致性**：`created_rows`/`work_dir`（Task 8↔13）、`row_to_dict`（Task 2↔10）、`search.embed_image/query/reindex`（Task 12↔13）签名核对一致；Task 13 明确要求给 Task 8 的 `_apply` 补 `_category/_table`。
4. 已知残留风险（执行时留意）：①四分类重导的"改价列/删行"坐标（harness 中 K 列/第 5 行）需按真实表头微调；②host.mjs 的插件挂载 sed 是脆弱点，部署后必须 grep 验证；③检索质量线（80/95）按 PRD，未达标走预留优化路线，不阻塞交付。

## Execution Handoff

计划完成，保存在 `docs/superpowers/plans/2026-08-29-dangkou-v2.md`。两种执行方式：

**1. Subagent-Driven（推荐）**——每任务派新 subagent，任务间两阶段 review，迭代快
**2. Inline Execution**——本会话内按 executing-plans 批量执行+检查点

选哪种？
