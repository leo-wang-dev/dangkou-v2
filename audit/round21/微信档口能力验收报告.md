# 微信档口能力验收报告

日期：2026-09-18  
结论：微信商家侧五项核心能力的代码链路已完成正向、反向和审批状态测试；离线全量回归通过。21:40 发现真实微信测试实例仍加载旧版“仅剃须刀/卷发棒”插件，随后完成服务器备份、同步、迁移和重启，并用微信已上传的吹风机文件完成动态分类真实导入。

## 本轮修复

1. 微信人格不再说“自定义分类尚未上线”。现在按“一个可见 Sheet=一个分类、表头=规格模板”说明，重复型号和空型号按原表保留。
2. 商家正式报价单的边界写清：已配置报价映射的预置剃须刀/卷发棒可出正式表；动态分类有多列成本/报价时不能猜价格列。
3. 微信插件在请求后端前校验空 Excel 路径、非法 docId/topK、空报价、非法数量/比例、缺 productId、空新增和空档口修改。
4. 图片上传和搜图改为校验真实文件内容，不再只信 `.jpg/.png` 后缀；限制 20MB、4000 万像素并识别真实格式。
5. 暂存图片保存为带正确扩展名的文件；图片读取接口按实际格式返回 Content-Type。

## 实际端到端能力矩阵

独立验证脚本使用真实 FastAPI 路由、SQLite、审批工单、openpyxl 和图片文件；只替换外部向量服务为本地确定性向量，不访问客户、真实模型或生产数据库。

| 能力 | 正向 | 反向 | 结果 |
|---|---|---|---|
| Excel 导入 | 1 Sheet/4 行/4 图，审批前不落库，批准后入库 | 损坏 xlsx、驳回、重复批准 | 通过 |
| 产品图 | 真实 `.bin` 内容识别为 JPEG，相同图命中，换一批排除 | 伪装图片、缺失路径、topK=0、模板外字段 | 通过 |
| 带图上新 | 图片+规格建审批，批准后入库并保持不可观测 | 审批前不变、空/非法输入不建工单 | 通过 |
| 商品查询 | 实时款数、全量型号、动态分类 | 未知分类 404，不把记录数当库存 | 通过 |
| 正式报价单 | 81 个按 40/箱进位到 120，文件可读并进入发送队列 | 空商品、0 数量、不存在商品、动态分类未映射 | 通过 |
| 档口资料 | 名称+TG+微信，驳回不变、批准生效 | 空操作、非法 TG、带换行微信号 | 通过 |
| 权限 | 带服务身份访问 | 错误身份返回 401 | 通过 |

端到端结果文件：`audit/round21/wechat-verification/result.json`，16 项通过、0 项失败。

## 回归结果

- Python 全量：`506 passed, 1 skipped`，耗时 162.68 秒。跳过项是环境条件用例；两条 warning 来自 TestClient/httpx 的依赖弃用提示。
- 微信能力重点回归：146 项通过，覆盖动态分类、双导入冲突、CRUD、搜图、报价、档口资料、红线和 H5 页面。
- 微信工具插件：7 个正向调用 + 8 个反向参数用例通过；原 10 个注册/调用契约通过。
- engine-v2：22 项通过，覆盖微信连接器、Token 专用拦截、通知文件安全、商家人格和普通商品消息路径。
- `git diff --check`、新增 Python 脚本编译检查通过。

## 线上补充验收

- 真实微信上传的原吹风机文件已在服务器识别为新动态分类，生成 4 条商品和 4 张图；重复型号与空型号保留。
- 真实模型新会话已验证收到同类 Excel 会调用 `catalog_import`，不会要求商家先开通品类；实时查询调用 `catalog_stats` 并回答 4 款。
- 导入完成通知已由微信通知队列发送成功。当前测试微信会话已在备份后重置，下一条真人消息会使用新版人格和工具定义。
- 详细证据见 `audit/round21/微信动态分类线上修复.md`。

## 仍需真人通过微信验证的部分

1. 同一真人微信继续发送不同措辞时，模型能否稳定映射到正确工具，尤其是“有几个”不能回答成库存、“要报价单”信息不足时必须追问。
2. 真实百炼向量对不同拍摄角度、背景和光线的 Top3 质量；自动测试只证明检索管道和相同图命中。
3. 导入完成通知、审批链接和报价文件是否真实到达微信；自动测试已验证持久队列、文件存在和失败重试语义。
4. 动态分类正式报价的价格列映射尚未定义。本轮明确拒绝猜列；例如吹风机同时有单风嘴、五风嘴、品质款成本时，需要产品规则决定用哪列以及对外模板。

人工验收直接使用 `docs/微信档口助手正反向手测清单.md` 和 `audit/round21/wechat-manual-kit/`。清单包含欢迎语、Excel、图片、查询、报价、档口资料、红线和 TG 教程的正反场景及失败判定。
