# TG 出表空白修复（2026-09-18）

用户真实发送 4 张照片后，两次发送“出表”（update 669210688、669210689）。日志显示两条指令均已处理，但仅返回本机网页链接。6 条识别结果均处于 draft，而网页、图片访问、修改、Excel 导出仅查询 confirmed，实际导出只有“序号、商品照片”两个表头。用户截图及 HTTP 访问日志确认复现。此前仅测“先确认再出表”，漏掉了真实用户直接出表路径。

## 修复

- TG 直接发送采购清单.xlsx，包含本客户全部 draft/confirmed 条目、图片和确认状态；不改变原始确认状态，不导出 discarded 或其他客户数据。
- 网页列表、图片、编辑、Excel 同步支持 draft，页面及 Excel 明确显示待确认。旧链接立即适用，无需重新上传或确认。
- 无任何条目时不生成空 Excel：bot 提示上传照片，网页禁用导出按钮，接口返回 409。
- tg_document 进入持久化发件箱，保存出表时字段快照，发送失败可重试；同会话文本等待附件发出；商家通知 worker 不消费 TG 附件。
- 网页与 TG 复用 Excel 渲染，保留动态字段、图片、公式注入防护。

## 验证

- 已向用户测试会话补发实际 Excel，Telegram message_id=17，文件 868815 字节；实际打开断言 6 条数据、6 张嵌入图片、6 条仍为草稿。证据：round7/tg-export-delivery.json。
- 用户原链接真实 Chromium 点击验证：显示 6 条待确认、6 图加载成功；修改起订量、刷新确认保存、恢复原值；点击导出下载实际 Excel，6 条 6 图；桌面和手机宽度截图及 trace 位于 round7/browser/。
- 完整 pytest：323 passed，3 skipped，0 failed。跳过的是显式关闭的真实网络测试；此次真实 TG 补发及实际页面验证单独执行。日志：round7/export-fix-final-tests.log。
- 新增覆盖：未确认直接出表、附件图片/状态、客户隔离、重复 update 幂等、发送失败重启重试及快照、通知 worker 分工、空清单、网页草稿编辑/图片/导出。

## 边界

当前网页仍是本机地址，远程手机直接访问需公网服务；TG 附件已经可直接接收。识别字段和价格仍需人工核对，文件生成成功不代表 OCR 内容已经确认。特别是本次日志中的 1236 等识别价格仍需对照原图核实，不能作为商家报价。未上线生产，未配置本测试库真实老板联系方式或商家通知服务。
