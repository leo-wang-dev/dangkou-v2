# 服务器联动只读核查

用户提供服务器信息后，通过 SSH 登录，只读检查了现有服务与默认数据文件。未修改服务、数据库或部署代码。

- dangkou2 服务 active，工作目录 /home/ubuntu/dangkou-v2。
- /home/ubuntu/dangkou-v2/data/catalog.db 包含 product_razor（30条）、product_curler（44条）、embedding、approval_ticket、import_doc（2条）。
- 上述数据库没有 cs_customer、cs_note、shop_profile、cs_inbox 等 C 端表；商品表也没有 cs_visible / tier_price 字段。
- 服务器目录不存在 scripts/run_cs_bot.py。本次读取的 .env 未见 TG_BOT_TOKEN、CATALOG_V2_DB 字段。尚未核实进程环境是否另行覆盖数据库路径。
- 本次用户 TG 测试实际运行在本机 audit/round7/live/catalog.db，与服务器库分离。

当前本机关系：TG 用户 → cs_customer → cs_note。来源档口四项信息在 fields_json 中，只支持照片字段提取/人工补录，没有 supplier_id 或 shop_id 外键。拍照询价另从当前连接的商品库找候选，客户选候选后读阶梯价；这不等于采购记录已关联实际档口。

结论：不能声称本轮 TG 已与生产档口端完成联动。服务器存在旧 B 端业务库，需要先核对运行配置并在隔离副本验证迁移、商家身份绑定与 C 端接入，再完成真实部署联调。采购照片不应因发送给某个 bot 就无条件当作该档口在售商品。
