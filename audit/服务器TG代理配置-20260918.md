# 服务器 Telegram 代理验证

2026-09-18，按用户授权，使用本机 ClashX Pro 当前配置中 Telegram 的选中节点。

- 主机：134.175.135.102。
- 客户端：Mihomo v1.19.31，下载文件 SHA256 与官方发布记录一致。
- 服务：`dangkou-vpn.service`，已启动并启用开机启动。
- 监听：仅 `127.0.0.1:17891`，没有开启 TUN 或修改系统默认路由。
- 私密配置：`/home/ubuntu/.local/share/dangkou-vpn/config.yaml`，权限 0600；节点凭据未写入仓库。
- Bot 应使用 `TG_PROXY_URL=socks5h://127.0.0.1:17891`，运行环境需要安装 requirements.txt 中的 PySocks。
- 已验证：服务器代理访问 Telegram 返回 HTTP 302；真实 bot getMe 成功；下载历史客户照片成功（123439 字节）；上传测试 Excel 成功（message_id=20）。测试文件明确标注为网络测试，不是采购清单。
- 原有 `dangkou2.service` 检查时仍为 active。未为此重启服务器。

当前复制的是有效节点配置快照，不是自动订阅更新。订阅方变更节点或凭据时需要重新同步。客户端和连接均在服务器运行，不依赖电脑或 SSH 网络转发。

本次证明了服务器 TG 网络与文件通道可用；不等于新版业务服务已经部署或业务验收完成。单 bot 的轮询进程切换仍需避免本地和服务器同时消费。
